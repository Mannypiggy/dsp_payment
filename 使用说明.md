# DSP 月度付款申请助手 · 使用说明

> 一句话：把 HUION 美国站 Amazon DSP 每月的付款申请流程自动化——
> 上传真实月度模板 → 一键处理 → 生成对账单/花费明细/店铺占比/付款申请邮件（含 Excel 截图）→ 打包 ZIP。

---

## 一、如何运行

```bash
cd D:\CodexProjects\11-DSP月度付款申请助手\dsp_payment

# 启动界面（或直接双击桌面「启动DSP付款.bat」）
streamlit run app.py

# 跑测试（回归网络）
python -m pytest tests/ -q
```

- 当前测试：**209 passed**（依赖本机 Excel COM + 桌面真实 2026-07 文件）。
- 依赖：`requirements.txt`（含 pywin32、Pillow；Excel 桌面版必须安装）。

---

## 二、页面要上传的文件

| 标号 | 文件 | 说明 |
|---|---|---|
| A | Amazon DSP Order Summary (*.xlsx) | DSP 数据真正来源 |
| B | 已推广广告数据（已推广-汇总 *.xlsx） | 站内数据真正来源 |
| C | 付款对账单（对账清单 *.xlsx） | 只核对最终付款金额 |
| D | DSP+SA 分析模板（对账单模板 *.xlsx） | 写入数据源 + 刷新 Pivot + 截图 |
| E | DSP花费明细模板 (*.xlsx) | 生成当月花费明细 |
| F | 店铺花费占比模板 (*.xls / *.xlsx) | 只改月份和申请费用 |
| G | DSP会议记录 (*.docx) | 生成 KPI 原因（需 AI Key，否则手动填） |
| H | Invoice (*.pdf) | 原样打包 |
| I | KPI 模板 (*.xlsx，可选) | 未上传不生成 KPI Excel / KPI 截图 |

页面还需确认：**截止付款日期**（手动填，未确认则邮件显示「待确认」+ 草稿）。

---

## 三、对账单模板——用哪一份（重要）

桌面 `C:\Users\41250\Desktop\2026年7月Huion-US对账单\2026年7月Huion-US对账单\` 里有多个版本，**只有一份正确**：

| 文件 | 大小 | 能否用 |
|---|---|---|
| ✅ **HUION-US-ENTITY-对账单-自用 -清空数据.xlsx** | 133801 | **用这份**（表头+Pivot 字段名正确，数据清空由工具填） |
| ✅ HUION-US-ENTITY-对账单-自用.xlsx | 168948 | 可用（带旧数据的原始版） |
| ❌ HUION-US-ENTITY-对账单-自用-7月.xlsx（测试文件夹里那个） | 114706 | **损坏**：Pivot 字段名变「字段1~字段15」→ 刷新报 #NAME? |
| ❌ HUION-US-ENTITY-对账单-自用-7月.xlsx.corrupted.bak | 114440 | **损坏**：表头被清空 |

> 程序已加两道前置校验：①数据源表头为空会报错；②Pivot 缓存出现「字段N」占位名会报错。
> 上传错模板会在处理一开始就明确提示，不会等到截图阶段。

---

## 四、关键业务口径（不要动）

- 金额（Decimal + ROUND_HALF_UP）：
  - `dsp_cost` = 49025.52（DSP 原始花费）
  - `rebate_amount` = 3922.04（返点）
  - `payment_amount` = 45103.48（扣除返点后的最终付款 = 对账单「7月结算金额」）
  - **DSP花费明细「申请支付金额」= payment_amount（45103.48）**（与对账单结算金额一致）
  - **店铺花费占比「申请费用总额」= payment_amount（45103.48）**
  - 邮件「付款金额」= 45103.48
- DSP花费明细：国家=美国、币种=USD、核算维度=店铺名（F 列两个合并区分别填哥贝尔店/DeePaint店，见 `config.local.yaml` 的 `store.stores`）、店铺品牌=HUION。
- KPI 区分：`calculated`（计算值，只作参考）vs `final`（用户/模板确认的最终汇报值，正式输出用它）。
- 邮件尾部顺序：申请原因 → 付款方 → 付款金额 → 截止付款时间 → 收款方信息 → 店铺花费占比。

---

## 五、邮件截图（最终阶段）

- 用 **Excel COM 真实渲染**后 CopyPicture 导出 PNG，禁止 pandas/matplotlib/HTML 重画。
- 截图：`01_DSP广告效果.png`、`02_DSP_KPI.png`（可选）、`03_站内广告效果.png`、`04_店铺花费占比.png`，打进 ZIP 的 `email_images/`。
- HTML 邮件用 base64 内嵌图片；TXT 用占位行。
- 空白图判定（`validate_png_content`）：unique_colors<20 或 non_white_ratio<0.01 或 灰度方差<10 → 判失败。
- 任何「本应存在」的截图失败 → 只出 `_调试包.zip`，不出正式 `DSP付款申请_YYYYMM.zip`。

---

## 六、今天（2026-08-30）修过的坑（下次遇到直接查）

1. **模板损坏**：表头被清空 / Pivot 缓存字段名变「字段N」。→ 换正确模板（见第三节）。
2. **Streamlit 非主线程 COM 失败**：必须 `pythoncom.CoInitialize()/CoUninitialize()`，否则报 `Open.RefreshAll`、`RPC_E_CALL_REJECTED(-2147418111)`。
3. **CopyPicture 导出空白图**：Excel 必须 `Visible=True` + `ScreenUpdating=True`（尤其旧版 .xls），否则白底无文字。
4. **截图漏「总计」行**：`find_range` 必须用 `UsedRange.Row/Column` 换算真实坐标，否则 off-by-one。
5. **第一列文字截断**：截图前临时 AutoFit 第一列，finally 恢复，不落盘。
6. **店铺占比月份错成上月**：写 COM 用 `datetime` 有时区偏移，改用 `month_rules.excel_date_serial`（纯序列）。
7. **店铺占比截图范围太大**：config `screenshots.spend_ratio.range = "A1:G7"` 只截主表。
8. **Pivot 刷新等待**：`CalculationState` 恒为 xlPending，用 `CalculateUntilAsyncQueriesDone()` 等待，别死等 CalculationState==0。
9. **文件被改名/移动**：对账单模板、会议记录路径曾在桌面被改，conftest/tests 已同步；下次换月记得更新路径。

---

## 七、项目结构

```
dsp_payment/
├─ app.py                    # Streamlit 主页面
├─ pipeline.py               # 一键编排（含截图、最终输出校验、ZIP 解压验收）
├─ models/payment_summary.py # 统一数据对象
├─ rules/                    # amount_fields / kpi_rules / month_rules / dsp_mapping / promoted_ads_mapping
├─ processors/               # order_summary / promoted_ads / reconciliation / spend_detail /
│                            #   spend_ratio / kpi / meeting / email / package / statement / validation
├─ utils/                    # excel_com / excel_screenshot / excel_utils / money / config_loader / llm_client / xls_utils
├─ config.local.yaml         # 真实配置（含 store.stores、screenshots 截图范围）
├─ output/runs/{run_id}/     # 每次运行独立目录，ZIP/PNG/HTML/TXT 都在这里
└─ tests/                    # 209 个测试
```
